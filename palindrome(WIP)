# Palindrome program

# -------------------------
# Subprograms
# -------------------------
def palindrome(phrase):
  message = []
  for i in phrase:
    if i.isalpha():
      message.append(i)
  print(message)
  if len(message)%2 > 0:

# -------------------------
# Main program
# -------------------------
i = str(input(""))
x = palindrome(i.lower())
